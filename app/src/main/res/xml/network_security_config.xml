<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base config: cleartext traffic denied by default.
         Only the domain-specific configs below permit HTTP. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Localhost / LAN: Stremio addons, TorrServer, and local scrapers
         often run on HTTP on the local network. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>

    <!-- Debug-only overrides for proxy/debugging tools -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user-installed CA certificates (e.g., Charles Proxy) -->
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
