package com.streame.tv.data.repository

import org.junit.Ignore
import org.junit.Test

class CloudSyncInvalidationBusTest {
    @Ignore("CloudSyncInvalidationBus and CloudSyncScope API changed; test needs rewrite")
    @Test
    fun `markDirty emits local invalidations`() {
        // Needs rewrite — CloudSyncInvalidationBus API has changed
    }

    @Ignore("CloudSyncInvalidationBus and CloudSyncScope API changed; test needs rewrite")
    @Test
    fun `remote apply suppresses invalidations and restores state`() {
        // Needs rewrite — CloudSyncInvalidationBus API has changed
    }
}
